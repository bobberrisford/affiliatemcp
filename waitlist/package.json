{
  "name": "affiliate-mcp-waitlist",
  "version": "0.1.0",
  "private": true,
  "description": "Waitlist-capture Cloudflare Worker for affiliate-mcp: accepts the pricing-page waitlist form and adds the address to a Resend audience. Holds no affiliate credentials or affiliate data.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run --outdir dist",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "wrangler": "^3.90.0"
  }
}
