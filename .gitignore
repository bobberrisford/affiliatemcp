node_modules/
dist/
coverage/
.env
.env.local
~/.affiliate-mcp/
.affiliate-mcp/
scratch/
.DS_Store
*.log
.idea/
.vscode/
# Local Claude Code state — but the contribute skill must ship (it
# auto-loads for contributors), so we ignore the noisy subpaths individually
# rather than ignoring `.claude/` wholesale.
.claude/cache/
.claude/sessions/
.claude/worktrees/
.claude/settings.local.json

# Local submission notes — not for the repo
SUBMISSION.md
